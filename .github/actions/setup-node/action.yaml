name: Setup Node
description: Setup Node.js environment

runs:
    using: composite
    steps:
        - uses: actions/setup-node@v5
          with:
              node-version: 22
              cache: npm
        - shell: bash
          run: npm ci
